# Configuração

Crie um evento `treasure_hunt`, defina `LOBBY`, `ENTRANCE` e `EXIT`, adicione etapas/objetivos e valide antes de abrir. Configurações específicas usam `treasure_hunt.finish-mode`, `treasure_hunt.ranking-mode` e `treasure_hunt.max-time-seconds`.
