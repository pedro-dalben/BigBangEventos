# Prompt: Treasure Hunt

Crie um módulo Fabric server-side `treasure_hunt` usando API 2. Preencha `{{STAGES}}`, `{{OBJECTIVES}}`, `{{PARTICIPANT_DATA}}`, `{{SESSION_DATA}}`, `{{DOMAIN_EVENTS}}` e `{{SCORE_RULES}}`. Use triggers genéricos e não importe BigBangRegions.
