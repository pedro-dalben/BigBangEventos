# Integração com BigBangEssentials

Opcional por contrato público. Futuros providers incluem `EconomyProvider`, `GemProvider`, `CrateProvider`, `RankProvider` e `PlayerProfileProvider`, todos com `isAvailable()` e resultado estruturado. O Essentials deverá publicar API estável; BigBangEventos não acessará campos privados nem classes internas.
